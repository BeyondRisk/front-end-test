import React from 'react'
import { IUserRow } from 'components/UserRow/UserRow'

// task 2, create a user list widget and display in storybook
// component should accept a list of users and display them (similar to CRUD table)
const ListUsers = ({ users, ...rest }: { users: IUserRow[]; rest?: any }) => {
  return <></>
}

export default ListUsers
